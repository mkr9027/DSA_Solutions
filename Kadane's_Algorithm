class Kadane's_Algorithm{
    int maxSubarraySum(int[] arr) {
        int max=arr[0];
        int ans=arr[0];
     
        for(int i=1;i<arr.length;i++){
            max=Math.max(max+arr[i],arr[i]);
            
            ans=Math.max(ans,max);
        }

        return ans;
    }
}